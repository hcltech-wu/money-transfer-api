# Application name
spring.application.name=money-transfer-service

# Server Configuration
server.port=8081

# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=moneyTransfer
# Uncomment if you want to use MongoDB Atlas
# spring.data.mongodb.uri=mongodb+srv://shaikkhasimbi:6addXreKM5BzgIfm@cluster0.imzufqp.mongodb.net/MoneytransferAPI?retryWrites=true&w=majority&appName=Cluster0

# Enable MongoDB debug (uncomment if needed)
# logging.level.org.mongodb.driver=DEBUG

# Use embedded MongoDB for development
spring.mongodb.embedded.version=6.0.11

# AWS S3 Configuration
aws.s3.bucket-name=transfer-receipts
aws.s3.region=us-east-1

# Exchange Rate API
exchange.api.url=https://api.exchangeratesapi.io/latest
exchange.api.key=your-api-key


# application.properties
springdoc.swagger-ui.path=/swagger-ui
springdoc.api-docs.path=/v3/api-docs
# Swagger Configuration - FIXED
# Remove the /123 from the path
#springdoc.api-docs.path=/api-docs
#springdoc.swagger-ui.path=/swagger-ui.html
#springdoc.swagger-ui.operationsSorter=method
#springdoc.swagger-ui.tagsSorter=alpha
#springdoc.api-docs.enabled=true
#springdoc.swagger-ui.enabled=true

# Add these properties to help with compatibility
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER
